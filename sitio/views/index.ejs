<!DOCTYPE html>
<html>
  <%- include('./partials/head.ejs')  %> <!-- HEAD -->
  <body>
    <%- include('./partials/header.ejs')  %> <!-- HEADER -->

    

<div class="container"><%- include('./partials/banner.ejs')  %> <!-- BANNER -->
  <div class="row destacados">
    <% productos.forEach(producto => { %>
      <% if(producto.discount > 50) { %> 
    <div class="col-6 col-md-12 col-lg-6 col-sm-12 destacado1">
      <figure>
        <a href=/products/detalle/<%= producto.id %> ><img src="/images/ropa/<%= producto.image %>" alt=""></a>
      </figure>
      <article class="nombredestacado">
        <h2><%= producto.name %></h2>
      </article>
      <article class="edicion">
        <h3>Limited Edition</h3>
      </article>
      <article class="comprar">
        <a href=""><button>COMPRAR</button></a>
      <article class="añadircarrito">
        <a href=""><button>AÑADIR AL CARRO</button></a>
      </article>
      <article class="descuentodestacado">
          $<%- (producto.price - producto.price * producto.discount / 100).toFixed(0).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".") %></i> <% if (producto.discount != 0) { %>
          <span><%= producto.discount %>%OFF</span>
        <% } %>
      </article>
    </div>
    <% } %> 
    <% }) %>
  </div>
</div>

<div class="container">
  <div class="row">
      <div class="col-12 titulo">
        </div>

        <% productos.forEach(producto => { %>
          <% if(producto.discount < 30 ) { %> 
    <div class="col-3 col-sm-12 col-md-6 col-lg-3 producto2">
      <a href=/products/detalle/<%= producto.id %> ><img src="/images/ropa/<%= producto.image %>" alt=""></a><i class="precioproducto">
          $<%- (producto.price - producto.price * producto.discount / 100).toFixed(0).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".") %></i><h3 class="descuentoproducto"> <% if (producto.discount != 0) { %>
          <span><%= producto.discount %>%OFF</span>
        <% } %></h3>
      <a href=""><i class="nombreproducto"><%= producto.name %></i></a>
    </div>
    <% } %> 
    <% }) %>
  </div>
</div>

<div class="container oferta">
  <div class="row">
    <div class="ofertas">
      OFERTAS
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
      <div class="col-12 titulo">
        </div>


        <% productos.forEach(producto => { %>
          <% if(producto.discount > 30 ) { %> 
    <div class="col-3 col-sm-12 col-md-6 col-lg-3 producto2">
      <a href=/products/detalle/<%= producto.id %> ><img src="/images/ropa/<%= producto.image %>" alt=""></a><i class="precioproducto">
          $<%- (producto.price - producto.price * producto.discount / 100).toFixed(0).toString().replace(/\B(?=(\d{3})+(?!\d))/g, ".") %></i><h3 class="descuentoproducto"> <% if (producto.discount != 0) { %>
          <span><%= producto.discount %>%OFF</span>
        <% } %></h3>
      <a href=""><i class="nombreproducto"><%= producto.name %></i></a>
    </div>
  <% } %> 
    <% }) %>
  </div>
</div>
  </body>
  <%- include('./partials/footer.ejs')  %> <!-- FOOTER -->
</html>
